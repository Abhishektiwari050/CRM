# Vercel Environment Variables
# Copy these to your Vercel dashboard under Project Settings > Environment Variables

# Database (Update with your actual database URL)
DATABASE_URL=postgresql://username:password@host:port/database

# Security (Generate a strong secret key)
SECRET_KEY=your-super-secret-key-at-least-32-characters-long

# CORS (Update with your actual frontend URL)
CORS_ORIGINS=https://your-frontend-domain.vercel.app,https://your-custom-domain.com

# Application Settings
DEBUG=false
LOG_LEVEL=INFO
ENABLE_RATE_LIMITING=true
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000

# Optional: Email Services (if using notifications)
# SENDGRID_API_KEY=your-sendgrid-api-key
# SENDGRID_FROM_EMAIL=your-from-email@domain.com

# Optional: AWS Services (if using AWS SES)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_REGION=us-east-1